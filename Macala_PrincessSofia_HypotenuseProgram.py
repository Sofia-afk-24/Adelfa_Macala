# ============================================================
# Name: Princess Sofia H. Macala
# Section: 8 Adelfa
# ============================================================

import math

a =float(input("Enter the length of side a: "))
b =float(input("Enter the length of side b: "))

hypotenuse =math.sqrt(pow(a,2)+pow(b,2))

print(f"The hypotenuse is: {hypotenuse:.2f}")
